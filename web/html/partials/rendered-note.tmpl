{{ define "rendered-note" }}

<div class="rendered-note">
  <div class="title-row">
    <h1> {{ .Title }}</h1>

    <div class="buttons">
      {{ if .IsPreview }}
      <button type="button" hx-target="body" hx-delete="/note/{{ .Id }}/{{ slugify .Title }}"  hx-confirm="Delete {{ .Title }}?" class="svg-button">
	<svg>
	  <use href="/static/vendor/svg/lucide-icons-sprite.svg#trash-2"/>
	</svg>
      </button>
      <button id="mde-preview-btn" type="button" class="svg-button">
	<svg class="icon">
	  <use href="/static/vendor/svg/lucide-icons-sprite.svg#eye-off"/>
	</svg>
      </button>
      <button type="submit" class="svg-button">
	<svg>
	  <use href="/static/vendor/svg/lucide-icons-sprite.svg#save"/>
	</svg>
      </button>
      {{ else }}
      <a class="svg-button" href="/edit/{{ .Id }}/{{ slugify .Title }}">
	<svg>
	  <use href="/static/vendor/svg/lucide-icons-sprite.svg#file-pen-line"/>
	</svg>
      </a>
      {{ end}}
    </div>
  </div>
  
  <details class="note-details">
    <summary>Details</summary>
    <dl class="details -compact">
      <dt><strong>Section:</strong></dt>
      <dd><a href="/section/{{ .Section.Id }}/{{ .Section.Name }}">{{ .Section.Name }}</a></dd>

      <dt><strong>Tags:</strong></dt>
      <dd>
	{{ range .Tags }}
	<a href="/tag/{{ .Id }}/{{ .Name }}">{{ .Name }}</a>
	{{ end }}
      </dd>

      <dt><strong>Last Updated:</strong></dt>
      <dd><time class="local-time" datetime="{{ .LastUpdatedAt }}"></time></dd>

      <dt><strong>Created:</strong></dt>
      <dd><time class="local-time" datetime="{{ .CreatedAt }}"></time></dd>
    </dl>
  </details>
  
  {{ .HTMLContent }}
  
</div>

{{ end }}
